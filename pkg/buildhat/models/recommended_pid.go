package models

// RecommendedPid represents recommended PID settings
type RecommendedPid struct {
	// Pid1 gets or sets the PID1
	Pid1 int
	// Pid2 gets or sets the PID2
	Pid2 int
	// Pid3 gets or sets the PID3
	Pid3 int
	// Pid4 gets or sets the PID4
	Pid4 int
}
